#include "main.h"

/**
 * main - check the code.
 * void: argument for main function.
 *
 * return: 0 if successful.
 */
int main(void)
{
	print_line(0);
	print_line(2);
	print_line(10);
	print_line(-4);
	return (0);
}
