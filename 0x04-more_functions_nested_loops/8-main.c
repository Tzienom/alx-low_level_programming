#include "main.h"

/**
 * main - check the code.
 * void: argument for main function.
 *
 * return: 0 if successful.
 */
int main(void)
{
	print_square(2);
	print_square(10);
	print_square(0);
	return (0);
}
