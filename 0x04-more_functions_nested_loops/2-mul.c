#include "main.h"

/**
 * mul - function for multiplying integers.
 * a: first argument for mul function.
 * b: second argument for mul function.
 *
 * return: 0 if successful.
 */
int mul(int a, int b)
{
	return a * b;
}
