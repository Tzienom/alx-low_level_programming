#include "main.h"

/**
 * main - check the code.
 * void: argument for main function.
 *
 * return: 0 if successful.
 */
int main(void)
{
	print_triangle(2);
	print_triangle(10);
	print_triangle(1);
	print_triangle(0);
	return (0);
}
